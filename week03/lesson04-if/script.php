<?php
$myCourse = "PHP Programming";

$num =7;
/* 
if ( $num % 2 == 0 ) {
    $text = "Even Number";
} else {
    $text = "Odd Number";
} 
*/

// Ternary Operator: 
$result = ($num % 2 == 0) ? 'your number is even' : 'your number is odd';

/*
NOTE:
As we learnt before, 
since this file contains ONLY php code (No HTML Template),
we can omit the closing php tag
*/